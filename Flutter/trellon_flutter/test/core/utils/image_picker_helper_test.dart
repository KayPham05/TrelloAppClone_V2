import 'package:flutter_test/flutter_test.dart';
import 'package:apptreolon/core/utils/image_picker_helper.dart';

void main() {
  test('ImagePickerHelper is a class', () {
    // Just a dummy test to allow SonarCloud to pick up the file,
    // actual testing of ImagePickerHelper requires platform channels mocking.
    expect(ImagePickerHelper, isNotNull);
  });
}
